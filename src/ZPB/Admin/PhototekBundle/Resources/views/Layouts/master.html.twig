{% extends "ZPBAdminCommonBundle:Layouts:master.html.twig" %}
{% block title %}La photothèque visiteurs{% endblock %}
{% set active_mainsidebar = active_mainsidebar|default("none") %}
{% set mainSection = mainSection|default("Photothèque") %}
{% block main %}
    <div class="container">
        <div class="row">
            <div class="column-12">
                <div class="page-header">
                    <h1>{{ mainSection }} <small>{% block page_small_title %}{% endblock %}</small></h1>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="column-3">
                {{ render(controller("ZPBAdminPhototekBundle:Navs:mainSidebar", {active_mainsidebar:active_mainsidebar })) }}
            </div>
            <div class="column-9">
                {% block content %}

                {% endblock %}
            </div>

        </div>
    </div>
{% endblock %}
